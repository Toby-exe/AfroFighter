#include "events.h"


enum avatarStates on_move (Avatar *player, UINT16 key)
{
    avatarStates state = Running;

    if(key = a_KEY)
    {
        
    }
    else if(key = d_KEY)
    {

    }

    return state;

}